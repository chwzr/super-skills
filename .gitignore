.worktrees/
.private-journal/
.claude/
# Super ephemeral runtime working dirs (brainstorm + sdd).
# Committed design docs live separately under .super/ and are NOT ignored.
.super-workspace/
.DS_Store
node_modules/
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md / README.md). It is not part of the published
# plugin, so the whole directory is ignored here.
evals/
